version: 2

builds:
  - binary: pulse
    goos:
      - darwin
      - linux
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{.Version}}

archives:
  - format: tar.gz
    name_template: "pulse_{{ .Os }}_{{ .Arch }}"

changelog:
  sort: asc
